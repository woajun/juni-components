import PointChart from './PointChart';

const ModalPage = () => {
  return (
    <div className="p-5">
      <PointChart data={[15, 20, 30, 50, 0, 0, 0, 0, 0, 0, 0]} />
    </div>
  );
};

export default ModalPage;
