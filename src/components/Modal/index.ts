import Modal from './Modal';
import Header from './ModalHeader';
import Body from './ModalBody';
import Footer from './ModalFooter';

export { programmaticModal } from './utils';

export default Object.assign(Modal, {
  Header,
  Body,
  Footer,
});
